# @protoform/auto-form

Compiled, engine-neutral AutoForm rendering and field logic. The host supplies a
`ProtoformUIComponentMap`; no shadcn implementation is bundled.

Most React Hook Form consumers should install `@protoform/react`, which composes this renderer with
`@protoform/core` and the React Hook Form adapter.
