// Self-contained tracer fixture for the plugin golden test. Kept
// annotation-free so it compiles without the protoform.v1 or
// buf/validate workspace imports. Rebuild the checked-in descriptor
// set with: bun run testdata:build (from this package).

// @generated by protoc-gen-protoform v__PLUGIN_VERSION__ with parameter "target=ts,import_extension=js"
// @generated from file tracer.proto (package protoform.test.v1, syntax proto3)
/* eslint-disable */

import { createProtoFormSchema, parseProtoSchema, protoToFormValues, registerProtoAnnotations } from "@/lib/protobuf-provider";
import { TracerChildSchema, TracerExampleSchema } from "./tracer_pb.js";

/**
 * Source documentation for protoform.test.v1.TracerChild.
 */
export const TracerChildFormAnnotations = {
  fields: {
    "protoform.test.v1.TracerChild.detail": "A detail shown with the trace.",
  },
  messages: {
    "protoform.test.v1.TracerChild": "Nested trace details.",
  },
} as const;
registerProtoAnnotations(TracerChildSchema, TracerChildFormAnnotations);

/**
 * Form binding for message protoform.test.v1.TracerChild.
 */
export const TracerChildFormBinding = {
  annotations: TracerChildFormAnnotations,
  createFormSchema: (options?: Parameters<typeof createProtoFormSchema>[1]) => createProtoFormSchema(TracerChildSchema, options),
  defaultValues: () => protoToFormValues(TracerChildSchema),
  descriptor: TracerChildSchema,
  parseSchema: () => parseProtoSchema(TracerChildSchema),
} as const;

/**
 * Source documentation for protoform.test.v1.TracerExample.
 */
export const TracerExampleFormAnnotations = {
  fields: {
    "protoform.test.v1.TracerExample.name": "The trace display name.",
    "protoform.test.v1.TracerExample.count": "The number of trace samples.",
    "protoform.test.v1.TracerExample.child": "Additional trace details.",
    "protoform.test.v1.TracerChild.detail": "A detail shown with the trace.",
  },
  messages: {
    "protoform.test.v1.TracerExample": "A trace configuration example.",
    "protoform.test.v1.TracerChild": "Nested trace details.",
  },
} as const;
registerProtoAnnotations(TracerExampleSchema, TracerExampleFormAnnotations);

/**
 * Form binding for message protoform.test.v1.TracerExample.
 */
export const TracerExampleFormBinding = {
  annotations: TracerExampleFormAnnotations,
  createFormSchema: (options?: Parameters<typeof createProtoFormSchema>[1]) => createProtoFormSchema(TracerExampleSchema, options),
  defaultValues: () => protoToFormValues(TracerExampleSchema),
  descriptor: TracerExampleSchema,
  parseSchema: () => parseProtoSchema(TracerExampleSchema),
} as const;
