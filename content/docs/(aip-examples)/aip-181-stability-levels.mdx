---
title: "AIP-181 Stability levels"
description: "Versioned descriptor type names and deprecation metadata produce explicit preview and migration guidance."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Versioned descriptor type names and deprecation metadata produce explicit preview and migration guidance.

[Read the canonical AIP-181 specification](https://google.aip.dev/181).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-181-stability-levels" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-181-stability-levels
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.181` — `registry/base-nova/protoform/lib/protobuf-provider/aip-client-workflow.test.ts`, “presents AIP-181 alpha, beta, stable, and deprecated guidance from generated descriptors”
