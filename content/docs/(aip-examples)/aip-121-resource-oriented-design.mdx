---
title: "AIP-121 Resource-oriented design"
description: "Resource descriptors remain the contract that drives form-visible resource fields."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Resource descriptors remain the contract that drives form-visible resource fields.

[Read the canonical AIP-121 specification](https://google.aip.dev/121).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-121-resource-oriented-design" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-121-resource-oriented-design
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.121` — `conformance/aip.conformance.test.ts`, “parses AIP-121/123 resource type, pattern, singular, and plural metadata”
