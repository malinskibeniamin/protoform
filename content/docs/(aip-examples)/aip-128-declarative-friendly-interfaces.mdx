---
title: "AIP-128 Declarative-friendly interfaces"
description: "Declarative mutation forms preserve validate_only while reconciliation remains server-owned."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Declarative mutation forms preserve validate_only while reconciliation remains server-owned.

[Read the canonical AIP-128 specification](https://google.aip.dev/128).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-128-declarative-friendly-interfaces" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-128-declarative-friendly-interfaces
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.128` — `conformance/aip.conformance.test.ts`, “models AIP-155 request IDs and AIP-163 validation previews as optional mutation controls”
