---
title: "AIP-236 Policy preview"
description: "Experiment forms keep preview metadata output-only, label preview as non-enforcing, and require confirmation plus etag for live commit."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Experiment forms keep preview metadata output-only, label preview as non-enforcing, and require confirmation plus etag for live commit.

[Read the canonical AIP-236 specification](https://google.aip.dev/236).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-236-policy-preview" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-236-policy-preview
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.236` — `conformance/aip-workflows.conformance.test.ts`, “models AIP-236 policy experiments and distinguishes preview from commit”
