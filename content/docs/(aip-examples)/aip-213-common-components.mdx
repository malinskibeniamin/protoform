---
title: "AIP-213 Common components"
description: "Forms use the standard protobuf well-known types rather than frontend-only replacements."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Forms use the standard protobuf well-known types rather than frontend-only replacements.

[Read the canonical AIP-213 specification](https://google.aip.dev/213).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-213-common-components" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-213-common-components
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.213` — `conformance/protovalidate-formats.conformance.test.ts`, “enforces duration const, comparisons, ranges, membership, normalization, and malformed input”
