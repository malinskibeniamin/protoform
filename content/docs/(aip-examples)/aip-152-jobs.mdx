---
title: "AIP-152 Jobs"
description: "Job resources expose descriptor-driven configuration and Run request forms that compose with the cancellable operation runner."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Job resources expose descriptor-driven configuration and Run request forms that compose with the cancellable operation runner.

[Read the canonical AIP-152 specification](https://google.aip.dev/152).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-152-jobs" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-152-jobs
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.152` — `conformance/aip-workflows.conformance.test.ts`, “models AIP-152 Job configuration and Run request forms”
