---
title: "AIP-136 Custom methods"
description: "The canonical SubmitBasicForm workflow proves a typed unary custom mutation without inventing a second RPC."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

The canonical SubmitBasicForm workflow proves a typed unary custom mutation without inventing a second RPC.

[Read the canonical AIP-136 specification](https://google.aip.dev/136).

## Try it

Edit the focused request, submit it, and compare the live behavior with the executable compatibility evidence below.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/aip-136-custom-methods" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/aip-136-custom-methods
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `aip.136` — `examples/basic/basic-form.test.tsx`, “submits a generated protobuf message to the local Connect server”
