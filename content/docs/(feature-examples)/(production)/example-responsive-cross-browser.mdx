---
title: "Responsive cross-browser forms"
description: "Keep generated forms usable across browser engines and narrow viewports."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Keep generated forms usable across browser engines and narrow viewports.


## Try it

Resize the page to a narrow viewport and complete the same form without horizontal loss.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="examples/kitchen-sink/kitchen-sink-form" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/responsive-cross-browser-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `production.cross-browser` — `scripts/browser-matrix.test.ts`, “runs the end-to-end form suite in Chromium, Firefox, and WebKit”
- `production.narrow-viewport` — `e2e/docs-screenshots.spec.ts`, “keeps the complex stepper usable at a narrow viewport”
