---
title: "Formik interoperability"
description: "Feed Protoform validation results into an existing Formik form without replacing Formik state."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Feed Protoform validation results into an existing Formik form without replacing Formik state.


## Try it

Submit invalid profile values and see Protoform paths surface through Formik.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/formik" />
</div>

## Form engine

This is an explicitly labeled Formik interoperability example; React Hook Form remains the default elsewhere.

## Install this example

```bash
bunx shadcn@latest add @protoform/formik-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `production.formik` — `examples/form-libraries/form-libraries.test.tsx`, “validates with Formik and submits a typed protobuf message”
