---
title: "Untrusted input hardening"
description: "Reject malformed descriptors, conversion values, and hostile server error paths without crashing the form."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Reject malformed descriptors, conversion values, and hostile server error paths without crashing the form.


## Try it

Use the safe live contract while executable evidence probes malformed values and paths.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/untrusted-input" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/untrusted-input-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `production.untrusted-input` — `conformance/untrusted-input.conformance.test.tsx`, “rejects malformed descriptors, conversion values, and server paths without throwing”
