---
title: "Sensitive-value redaction"
description: "Keep sensitive input available for submission while redacting it from review output."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Keep sensitive input available for submission while redacting it from review output.


## Try it

Enter a token and inspect the review state, which never echoes the secret.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="examples/complex/complex-form" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/credential-redaction-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `production.credential-redaction` — `examples/complex/complex-form.test.tsx`, “redacts credentials from the final review summary”
