---
title: "Performance and bundle budgets"
description: "Stay within large-form interaction and tree-shaken bundle budgets."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Stay within large-form interaction and tree-shaken bundle budgets.


## Try it

Add and edit collection rows while the automated budgets guard render and bundle regressions.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="examples/kitchen-sink/kitchen-sink-form" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/performance-bundle-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `production.performance` — `conformance/performance.conformance.test.tsx`, “keeps render, validation, field change, and step transition within budgets for a $fields-field descriptor”
- `production.bundle-size` — `scripts/bundle-budget.test.ts`, “enforces registry runtime and example budgets”
