---
title: "Bytes and Any rules"
description: "Validate base64 bytes and Any type URLs against exact, length, pattern, allow, and deny rules."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Validate base64 bytes and Any type URLs against exact, length, pattern, allow, and deny rules.


## Try it

Change the base64 and type URL inputs to trigger safe validation failures.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protovalidate-bytes-any" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protovalidate-bytes-any-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protovalidate.bytes` — `conformance/protovalidate-formats.conformance.test.ts`, “validates the %s bytes rule”
- `protovalidate.any` — `conformance/protovalidate-matrix.conformance.test.ts`, “enforces Any type URL allow and deny lists and rejects malformed form values”
