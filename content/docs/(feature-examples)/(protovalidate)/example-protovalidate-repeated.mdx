---
title: "Repeated-field rules"
description: "Apply repeated-item rules, cardinality limits, uniqueness, and indexed error paths."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Apply repeated-item rules, cardinality limits, uniqueness, and indexed error paths.


## Try it

Add duplicate and invalid rows, then remove one and confirm indexed errors follow the data.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protovalidate-repeated" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protovalidate-repeated-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protovalidate.repeated-items` — `conformance/protoform.conformance.test.ts`, “returns form-shaped paths for $name failures”
- `protovalidate.repeated-cardinality` — `conformance/protovalidate-matrix.conformance.test.ts`, “enforces repeated min, max, and uniqueness while allowing duplicate messages”
