---
title: "Required values and oneofs"
description: "Show required scalar and required oneof violations at their exact form paths."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Show required scalar and required oneof violations at their exact form paths.


## Try it

Clear required fields and submit to see every missing value, then select a oneof branch.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protovalidate-required" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protovalidate-required-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protovalidate.required` — `conformance/protoform.conformance.test.ts`, “returns form-shaped paths for $name failures”
- `protovalidate.oneof-required` — `conformance/protoform.conformance.test.ts`, “returns form-shaped paths for $name failures”
