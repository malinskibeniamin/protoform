---
title: "Well-known types"
description: "Edit Timestamp, Duration, and FieldMask values with purpose-built controls."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Edit Timestamp, Duration, and FieldMask values with purpose-built controls.


## Try it

Change time, duration, and mask values, then submit their protobuf representation.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protobuf-well-known-types" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protobuf-well-known-types-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protobuf.timestamp` — `registry/base-nova/protoform/lib/protobuf-provider/index.test.ts`, “converts protobuf messages into form-friendly values”
- `protobuf.duration` — `registry/base-nova/protoform/lib/protobuf-provider/index.test.ts`, “converts protobuf messages into form-friendly values”
- `protobuf.field-mask` — `registry/base-nova/protoform/lib/protobuf-provider/index.test.ts`, “converts protobuf messages into form-friendly values”
