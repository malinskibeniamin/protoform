---
title: "Recursive messages"
description: "Render recursive descriptors safely without expanding an unbounded field tree."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Render recursive descriptors safely without expanding an unbounded field tree.


## Try it

Edit the bounded recursive node and submit without triggering an infinite render.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protobuf-recursive-messages" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protobuf-recursive-messages-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protobuf.recursive-messages` — `conformance/recursive-messages.conformance.test.ts`, “bounds self-referential message and repeated-message expansion with a JSON leaf”
