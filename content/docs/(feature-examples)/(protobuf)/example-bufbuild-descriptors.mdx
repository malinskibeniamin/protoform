---
title: "Bufbuild service descriptors"
description: "Use generated service and RPC descriptors to keep a form tied to its request contract."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Use generated service and RPC descriptors to keep a form tied to its request contract.


## Try it

Complete the request form that a generated service method accepts.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/bufbuild-descriptors" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/bufbuild-descriptors-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protobuf.services` — `registry/base-nova/protoform/lib/protobuf-provider/method-workflow.test.ts`, “getProtoMethodWorkflow”
