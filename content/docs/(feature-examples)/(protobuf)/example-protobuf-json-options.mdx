---
title: "ProtoJSON and custom options"
description: "Round-trip ProtoJSON while descriptor custom options continue to drive labels, controls, and help."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Round-trip ProtoJSON while descriptor custom options continue to drive labels, controls, and help.


## Try it

Edit annotated fields and submit to inspect the JSON-safe message shape.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protobuf-json-options" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protobuf-json-options-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protobuf.json-round-trip` — `registry/base-nova/protoform/hooks/use-proto-form/use-proto-form.test.ts`, “produces a message that survives JSON round-trip”
- `protobuf.custom-options` — `registry/base-nova/protoform/lib/protobuf-provider/render-hints.test.ts`, “specific annotation values map onto the right hint properties”
