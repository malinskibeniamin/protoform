---
title: "Presence and wrapper values"
description: "See how optional fields, wrapper messages, and protobuf default values stay distinguishable in form state."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

See how optional fields, wrapper messages, and protobuf default values stay distinguishable in form state.


## Try it

Toggle populated values and submit to compare explicit presence with an unset field.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/protobuf-presence" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/protobuf-presence-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `protobuf.optional` — `conformance/protoform.conformance.test.ts`, “round-trips optional, wrapper, JSON, map, bytes, and oneof values”
- `protobuf.presence-defaults` — `conformance/protobuf-matrix.conformance.test.ts`, “distinguishes explicit presence while documenting implicit scalar loss”
- `protobuf.wrappers` — `conformance/protoform.conformance.test.ts`, “round-trips optional, wrapper, JSON, map, bytes, and oneof values”
- `protobuf.wrapper-matrix` — `conformance/protobuf-matrix.conformance.test.ts`, “round-trips every remaining scalar wrapper with unset and default values”
