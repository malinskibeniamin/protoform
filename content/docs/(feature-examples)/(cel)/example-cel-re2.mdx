---
title: "CEL strings and RE2"
description: "Use CEL string helpers and RE2-compatible regular expressions with portable validation messages."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Use CEL string helpers and RE2-compatible regular expressions with portable validation messages.


## Try it

Try an uppercase project ID, then a lowercase hyphenated value that matches RE2.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="examples/learning/cel-re2-form" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/cel-re2-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `cel.strings` — `conformance/cel-language.conformance.test.ts`, “supports string and bytes operations”
