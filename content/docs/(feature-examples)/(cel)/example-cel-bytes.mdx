---
title: "CEL byte values"
description: "Evaluate CEL byte literals and byte operations without treating bytes as ordinary Unicode text."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Evaluate CEL byte literals and byte operations without treating bytes as ordinary Unicode text.


## Try it

Change the encoded payload and submit to exercise byte-aware rules.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/cel-bytes" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/cel-bytes-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `cel.bytes` — `conformance/cel-language.conformance.test.ts`, “supports string and bytes operations”
