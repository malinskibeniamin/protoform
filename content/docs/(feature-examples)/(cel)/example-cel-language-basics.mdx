---
title: "CEL language basics"
description: "Evaluate CEL literals, scalar types, logical, relational, arithmetic, and explicit conversion expressions."
---

{/* Generated by scripts/generate-demo-catalog.ts. */}

Evaluate CEL literals, scalar types, logical, relational, arithmetic, and explicit conversion expressions.


## Try it

Change scalar values so the cross-field expression moves between valid and invalid states.

<div className="my-8 border-y border-border/60 py-8">
  <Component path="registry/base-nova/protoform/demo/catalog/cel-language-basics" />
</div>

## Form engine

This example uses Protoform's default React Hook Form adapter.

## Install this example

```bash
bunx shadcn@latest add @protoform/cel-language-basics-demo
```

The registry item installs this focused example and its shared demo runtime. Copy it, rename it, and
replace the fixture contract with your own generated descriptor.

## Executable compatibility evidence

The live form is visual evidence. These automated checks prove the underlying contract:

- `cel.syntax-literals` — `conformance/cel-language.conformance.test.ts`, “supports CEL syntax, literals, scalar types, operators, and conversions”
- `cel.scalar-types` — `conformance/cel-language.conformance.test.ts`, “supports CEL syntax, literals, scalar types, operators, and conversions”
- `cel.operators` — `conformance/cel-language.conformance.test.ts`, “supports CEL syntax, literals, scalar types, operators, and conversions”
- `cel.conversions` — `conformance/cel-language.conformance.test.ts`, “supports CEL syntax, literals, scalar types, operators, and conversions”
