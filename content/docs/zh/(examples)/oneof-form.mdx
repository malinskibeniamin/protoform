---
title: Oneof 表单
description: "在两个标量 oneof 分支之间切换，且不保留隐藏值。本指南进一步说明如何在 Protoform 中实际应用此功能，并涵盖契约设计、表单生成、字段验证、错误处理、迁移策略、无障碍体验、测试方法与生产环境集成，帮助团队采用经过验证且可维护的实现路径。"
---
**第 5 级，共 8 级**

**前置条件：** [CEL 和 RE2 表单](/docs/cel-re2-form)

本示例用整个屏幕展示一个概念：protobuf `oneof` 会转换为分支选择器，后面跟随所选分支拥有的字段。

<div className="my-8 border-y border-border/60 py-8">
  <OneofFormExample />
</div>

## 新增内容

- 包含两个标量分支的必填 `oneof`。
- 提交的消息中仅包含一个类型明确的活动分支。
- 所选分支更改时自动清理。

## 契约

```proto
message OneofForm {
  oneof contact {
    option (buf.validate.oneof).required = true;
    option (protoform.v1.oneof_ui).description = "Choose one contact method.";

    string email = 1;
    string phone = 2;
  }
}
```

输入电子邮件地址，切换到“电话”，然后提交。类型化消息仅包含电话分支；隐藏的电子邮件地址会被清除，而不会成为幽灵数据。

**下一步：** [服务器错误表单](/docs/server-error-form)

{/* docs-example-context */}
这个聚焦示例说明字段如何从 protobuf 契约进入可访问表单，以及提交、验证和错误状态如何保持类型安全。请尝试有效值、边界值和失败路径，再查看相邻指南中的安装步骤与自动化证据，以确认同一模式适用于真实应用。

{/* docs-cjk-checklist */}
## 验证演示的步骤

1. 对照 `protobuf schema`、生成的 `descriptor`、`field config` 与 `registry item`，确认 字段 名称、presence 语义、默认值 和 oneof 分支 保持 一致。
2. 依次 尝试 正常 输入、边界 输入、空值、重复 项目 和 无效 格式，确认 客户端 校验 展示 所有 可操作 错误。
3. 切换 oneof 分支、删除 嵌套 消息、编辑 repeated message 和 map value，确认 旧分支 数据 不会 在提交时 恢复。
4. 检查 键盘 导航、焦点 顺序、ARIA 属性、错误 摘要 与 提交 状态，确保 辅助 技术 能够 理解 每个 控件。
5. 对比 提交 前后的 protobuf message，确认 已知 字段 正确 更新，未知 wire 字段 在未修改的 嵌套 结构 中 保留。
6. 最后 运行 registry 安装、类型 检查、单元 测试 与 集成 测试，再把 相同 契约 用于 真实 应用。
