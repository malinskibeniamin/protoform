diff --git a/dist/cli/index.js b/dist/cli/index.js
index 3174de9fae86b7dbaf57d5bc775def5e4c1361ac..5a0f0a0911e0dc468c512b79096ae35c796e1d9d 100755
--- a/dist/cli/index.js
+++ b/dist/cli/index.js
@@ -4284,9 +4284,9 @@ ${userConfigSetup}export default defineConfig({
     // SSR entry is emitted as \`index.mjs\` instead of the \`entry.mjs\` the
     // Vercel adapter's \`astro:build:done\` hook then fails to find. \`prerender\`
     // is Astro-only and has no legacy equivalent, so it stays under \`environments\`.
-    ssr: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)} },
+    ssr: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)}, noExternal: ["js-yaml"] },
     environments: {
-      prerender: { resolve: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)} } },
+      prerender: { resolve: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)}, noExternal: ["js-yaml"] } },
     },
     resolve: {
       alias: {
diff --git a/src/astro/templates.ts b/src/astro/templates.ts
index 402150fffae61fae9630651f429960b7adc9197c..3def156e5c8c2ef7e0be2d8d7b94314deeb36e95 100644
--- a/src/astro/templates.ts
+++ b/src/astro/templates.ts
@@ -806,9 +806,9 @@ ${userConfigSetup}export default defineConfig({
     // SSR entry is emitted as \`index.mjs\` instead of the \`entry.mjs\` the
     // Vercel adapter's \`astro:build:done\` hook then fails to find. \`prerender\`
     // is Astro-only and has no legacy equivalent, so it stays under \`environments\`.
-    ssr: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)} },
+    ssr: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)}, noExternal: ["js-yaml"] },
     environments: {
-      prerender: { resolve: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)} } },
+      prerender: { resolve: { external: ${JSON.stringify(RENDER_EXTERNAL_DEPS)}, noExternal: ["js-yaml"] } },
     },
     resolve: {
       alias: {
