syntax = "proto3";

package protoform.examples.v1;

import "buf/validate/validate.proto";

message CelRe2Form {
  string project_id = 1 [(buf.validate.field).cel = {
    id: "project_id.re2"
    message: "Use lowercase letters, digits, and hyphens."
    expression: "this.matches('^[a-z][a-z0-9-]*$')"
  }];
}
